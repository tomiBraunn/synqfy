# Spotify
SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=
SPOTIFY_REDIRECT_URI=http://localhost:3001/auth/callback

# Home Assistant
HA_URL=http://homeassistant.local:8123
HA_TOKEN=
# Which lamps take each color is picked in the app (Luces tab). These only seed
# the first run — once saved from the page, the page wins.
HA_ENTITY_IDS_PRIMARY=light.sala
HA_ENTITY_IDS_SECONDARY=light.cocina

# App
PORT=3001
FRONTEND_URL=http://localhost:5173
